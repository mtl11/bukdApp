import React, { useState } from "react";
import {
  View,
  Text,
  SafeAreaView,
  StyleSheet,
  Icon,
  TouchableOpacity,
  Image,
  TextInput,
  Button,
} from "react-native";

const MessageScreen = (props) => {
  return (
    <SafeAreaView>
      <View></View>
    </SafeAreaView>
  );
};

export default MessageScreen;