export default class profileInformation {
    constructor(profileImgURL, username, genre, subgenre, bio){
        this.profileImgURL = profileImgURL;
        this.username = username;
        this.genre = genre;
        this.subgenre = subgenre;
        this.bio = bio;
    }
}